import { Test, TestingModule } from '@nestjs/testing';
import { ConfigModule } from '@nestjs/config';
import { config } from '@tresdoce-nestjs-toolkit/test-utils';

import { {{pascalCase moduleName}}Module, {{pascalCase moduleName}}Service } from '..';

describe('{{pascalCase moduleName}}Service', () => {
  let service: {{pascalCase moduleName}}Service;

  beforeEach(async () => {
    const module: TestingModule = await Test.createTestingModule({
      imports: [
        ConfigModule.forRoot({
          isGlobal: true,
          load: [config],
        }),
        {{pascalCase moduleName}}Module,
      ],
      providers: [{{pascalCase moduleName}}Service],
    }).compile();

    service = module.get<{{pascalCase moduleName}}Service>({{pascalCase moduleName}}Service);
  });

  it('should be defined', () => {
    expect(service).toBeDefined();
  });

  it('should be return message when call the getHello function', () => {
    expect(service.getHello()).toBe(
      `¡Hello ${config().project.name} from the new module {{kebabCase moduleName}}!`,
    );
  });
});
